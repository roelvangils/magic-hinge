# Editable icon vectors

The current icon uses `05-folded-glass.svg`, based on the user-supplied shape.
Its short center bar and outline both use an 11-unit stroke before scaling.
The Icon Composer document is `../Magic Hinge.icon`. Native lighting and shadows
are applied by Icon Composer; the SVG contains editable gradients and geometry.

The earlier 01–04 laptop exploration files are unused development artwork.
